# Task List - Botões de Retorno ("Voltar")

- `[x]` **Navegação & Interface XML**
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_formulario_materia.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_nova_sessao.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_cronometro.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_tecnicas.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_sobre.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_perfil.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_metas.xml`
    - `[x]` Add Back Button (`btnVoltar`) to `fragment_relatorios.xml`
- `[x]` **Lógica em Java (Fragments)**
    - `[x]` Handle `btnVoltar.setOnClickListener` with `Navigation.findNavController(view).navigateUp()` in all secondary Fragments
- `[x]` **Verificação**
    - `[x]` Gradle build verification (`app:assembleDebug`) -> BUILD SUCCESSFUL!
